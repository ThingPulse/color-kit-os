#ifndef _WEATHER_IMAGE_H
    #define _WEATHER_IMAGE_H

    void weather_image_tile_setup( uint32_t tile_num );

    void update_weather_detail(weather_config_t *weather_config, weather_forcast_t * hourly_forecast);

#endif

